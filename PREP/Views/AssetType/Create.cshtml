@model PREP.Models.AssetType

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AssetType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Asset)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Asset)
            @Html.ValidationMessageFor(model => model.Asset)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PicturePath)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PicturePath)
            @Html.ValidationMessageFor(model => model.PicturePath)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
